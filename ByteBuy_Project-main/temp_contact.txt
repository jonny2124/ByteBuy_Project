/* ===============================
   CONTACT PAGE 
   =============================== */

:root {
  --primary-yellow: #FFD300;
  --accent-blue: #2563EB;
  --accent-orange: #FF8C00;
  --dark-bg: #0F0F0F;
  --grey-bg: #F2F2F2;
  --text-dark: #111827;
  --text-light: #FFFFFF;
}

/* HERO */
.contact-hero {
  position: relative;
  height: 48vh;
  min-height: 380px;
  background:
    radial-gradient(1200px 400px at 10% 10%, rgba(37,99,235,0.25), transparent 90%),
    radial-gradient(1200px 400px at 90% 90%, rgba(255,211,0,0.25), transparent 90%),
    #0F0F0F;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--text-light);
  text-align: center;
}

.contact-hero-overlay { max-width: 960px; padding: 0 16px; }

.contact-hero-overlay h1 {
  font-size: clamp(2.4rem, 4vw + 1rem, 4rem);
  line-height: 1.1;
  color: var(--primary-yellow);
  margin-bottom: 12px;
  letter-spacing: 0.2px;
  text-shadow:
    0 10px 30px rgba(255, 211, 0, 0.35),
    0 4px 14px rgba(37, 99, 235, 0.25),
    0 2px 6px rgba(0, 0, 0, 0.6);
}

.contact-hero-overlay h1::after {
  content: "";
  display: block;
  width: 120px;
  height: 4px;
  margin: 10px auto 0;
  border-radius: 999px;
  background: linear-gradient(90deg, var(--primary-yellow), var(--accent-blue));
  box-shadow: 0 0 20px rgba(255, 211, 0, 0.6);
}

.contact-hero-overlay p {
  font-size: clamp(1.05rem, 1.1vw + 0.6rem, 1.35rem);
  color: #f5f5f5;
  display: inline-block;
  padding: 8px 14px;
  background: rgba(0, 0, 0, 0.35);
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: 12px;
  text-shadow: 0 2px 8px rgba(0, 0, 0, 0.45);
}

/* QUICK CHANNELS */
.contact-channels {
  padding: 36px 5% 10px;
}

.channels-grid {
  display: grid;
  gap: 16px;
  grid-template-columns: repeat(4, minmax(0, 1fr));
}

.channel-card {
  background: #fff;
  border: 1px solid #eee;
  border-radius: 14px;
  padding: 18px;
  text-decoration: none;
  color: inherit;
  display: flex;
  flex-direction: column;
  gap: 8px;
  transition: transform .2s ease, box-shadow .2s ease, border-color .2s ease;
}

.channel-card:hover {
  transform: translateY(-4px);
  border-color: var(--primary-yellow);
  box-shadow: 0 12px 24px rgba(0,0,0,.08);
}

.channel-card .icon {
  width: 40px;
  height: 40px;
  border-radius: 10px;
  display: grid;
  place-items: center;
  background: rgba(37,99,235,0.08);
  color: #111;
  font-weight: 800;
}

.channel-card h3 { font-size: 1.05rem; }
.channel-card p { color: #555; font-size: .95rem; }

/* MAIN LAYOUT */
.contact-main { padding: 20px 5% 70px; }

.contact-container {
  display: grid;
  grid-template-columns: 1.5fr .9fr;
  gap: 26px;
  align-items: start;
}

/* FORM */
.contact-form-wrap {
  background: #fff;
  border: 1px solid #eee;
  border-radius: 16px;
  padding: 24px;
  box-shadow: 0 10px 16px rgba(0,0,0,0.05);
}

.contact-form-wrap h2 { margin-bottom: 6px; }
.contact-form-wrap .sub { color: #666; margin-bottom: 14px; }

.contact-form {
  display: flex;
  flex-direction: column;
  gap: 14px;
}

.form-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 14px;
}

.field { display: flex; flex-direction: column; gap: 6px; }

.field label { font-weight: 600; font-size: .95rem; }

.field input,
.field select,
.field textarea {
  border: 1px solid #ddd;
  border-radius: 10px;
  padding: 10px 12px;
  outline: none;
  font-family: inherit;
  transition: border-color .2s ease, box-shadow .2s ease;
}

.field textarea { resize: vertical; }

.field input:focus,
.field select:focus,
.field textarea:focus {
  border-color: var(--accent-blue);
  box-shadow: 0 0 0 3px rgba(37,99,235,0.12);
}

.muted { color: #777; font-weight: 400; }

.form-options {
  display: flex;
  gap: 18px;
  flex-wrap: wrap;
  align-items: center;
}

.checkbox { display: inline-flex; align-items: center; gap: 8px; color: #333; }

/* ASIDE */
.contact-aside {
  background: #fff;
  border: 1px solid #eee;
  border-radius: 16px;
  padding: 20px;
}

.persona-cards { display: grid; gap: 12px; }
.persona-card {
  border: 1px solid #eee;
  border-radius: 12px;
  padding: 14px;
  background: #fafafa;
}
.persona-card h4 { font-size: 1rem; margin-bottom: 6px; }
.persona-card p { color: #555; margin-bottom: 10px; }

.pill {
  display: inline-block;
  padding: 8px 12px;
  background: var(--accent-blue);
  color: #fff; text-decoration: none;
  border-radius: 999px; font-weight: 700; font-size: .9rem;
}

.contact-meta { margin-top: 14px; font-size: .95rem; color: #444; }

/* FAQ */
.contact-faq { padding: 20px 5% 60px; }
.contact-faq h2 { text-align: center; margin-bottom: 16px; }
.faq-list { max-width: 900px; margin: 0 auto; display: grid; gap: 10px; }
.faq-list details {
  background: #fff; border: 1px solid #eee; border-radius: 12px; padding: 12px 16px;
}
.faq-list summary { cursor: pointer; font-weight: 600; }
.faq-list p { color: #444; margin-top: 8px; }

/* LOCATION */
.contact-location { padding: 0 5% 80px; }
.location-card {
  background: #fff; border: 1px solid #eee; border-radius: 16px; padding: 18px;
  display: grid; grid-template-columns: 1fr 1fr; gap: 16px; align-items: center;
}
.location-info h3 { margin-bottom: 6px; }
.location-info .muted { color: #666; }
.location-map {
  background: repeating-linear-gradient(45deg, #f3f3f3, #f3f3f3 10px, #ececec 10px, #ececec 20px);
  border-radius: 12px; height: 220px; display: grid; place-items: center; color: #666;
}

/* RESPONSIVE */
@media (max-width: 1100px) {
  .channels-grid { grid-template-columns: repeat(2, minmax(0,1fr)); }
  .contact-container { grid-template-columns: 1fr; }
}

@media (max-width: 640px) {
  .form-row { grid-template-columns: 1fr; }
  .location-card { grid-template-columns: 1fr; }
}
